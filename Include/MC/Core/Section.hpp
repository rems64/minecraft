#pragma once

#include <string>
#include <vector>
#include <map>

namespace MC
{
	class Section
	{
	public:
		Section();
		~Section();

	private:
		//std::map<std::string, unsigned short int> m_dict;
		//std::vector<unsigned short int> m_blocks;
	};
}